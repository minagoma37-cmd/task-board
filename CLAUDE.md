# CLAUDE.md

このファイルは、このプロジェクトで Claude / コーディングエージェントが作業するときの指針です。

## プロジェクト概要

このプロジェクトは、React で作成する個人練習用のタスクボードアプリです。
大規模なアプリではなく、React の基本的な状態管理・イベント処理・コンポーネント設計を練習するための小さなアプリとして扱ってください。

## 実装する機能

- テキスト入力でタスクを追加できる
- チェックボックスでタスクの完了・未完了を切り替えられる
- タスクを削除できる
- 完了済みのタスクはグレーで表示する

## スコープ外

以下のような複雑な機能は、明示的に依頼されない限り追加しないでください。

- ユーザー認証
- データベース連携
- API サーバー
- タスクのドラッグアンドドロップ
- 期限、タグ、カテゴリ、優先度
- 複雑なフィルターや検索
- 状態管理ライブラリの追加
- UI コンポーネントライブラリの追加

## 開発方針

- シンプルで読みやすい React コードを優先してください。
- 個人練習用なので、過度な抽象化や大規模構成にしないでください。
- まずはローカル state で十分です。必要がない限り Redux、Zustand、Context などは使わないでください。
- コンポーネントは必要になった時点で分割し、最初から細かく分けすぎないでください。
- 既存のコードや構成がある場合は、そのスタイルに合わせてください。
- 依存パッケージの追加は最小限にしてください。
- コメントは、コードの意図が読み取りにくい箇所にだけ簡潔に追加してください。

## UI 方針

- 画面はタスクの追加・完了切り替え・削除がすぐ分かる構成にしてください。
- 完了済みタスクは、文字色をグレーにするなど視覚的に区別してください。
- 練習用アプリとして、派手な装飾よりも分かりやすさを優先してください。
- ただし最低限、スマートフォンとデスクトップの両方で崩れないようにしてください。

## Git / GitHub

- GitHub への push は、ユーザーから明示的に指示されない限り実行しないでください。
- `git push`、PR 作成、リモートブランチ操作は勝手に行わないでください。
- 既存のユーザー変更を勝手に戻さないでください。

## 作業後の確認

変更後は、可能であれば以下を確認してください。

- アプリが起動すること
- タスクを追加できること
- チェックボックスで完了状態を切り替えられること
- 削除ボタンでタスクを削除できること
- 完了済みタスクがグレーで表示されること

## Githubリポジトリ
https://github.com/minagoma37-cmd/task-board.git

## デプロイ先

https://minagoma37-cmd.github.io/task-board/

## 技術スタック

- React
- JavaScript
- Vite
- CSS
- localStorage
- Git / GitHub
- GitHub Pages

## コンポーネントの命名規約

- ReactコンポーネントはPascalCaseで命名する
- 例：App、TaskItem
- コンポーネントのファイル名もPascalCaseにする
